@model IEnumerable<NTNUntappd.Models.CheckInModels>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>CheckIns</th>
        <th>Count</th>
    </tr>
    
@if (Model.Count() > 0)
{
    var groups = Model.GroupBy(checkIn => checkIn.Beer.Name, checkIn => checkIn.Id);

    foreach (var group in groups)
    {

        var beer = group.Key;
        <tr>
            <td>
                @Html.DisplayFor(g => group.Key)
            </td>
            <td>
                @Html.DisplayName("" + group.Count())
            </td>
            <td>
            </td>
        </tr>
        
    }
    /*
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Beer.Name)
            </td>
            <td></td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }*/
}



</table>
